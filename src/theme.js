import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
    palette: {
        primary: { main: '#8B1A1A' },
        background: { default: '#FDF6EE' },
    },
    typography: {
        fontFamily: "'DM Sans', sans-serif",
    },
})